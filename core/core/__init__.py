# Marks `core/` as a Python package, enabling `from core.config import ...` etc.
# Empty __init__.py is the Python equivalent of a package-info.java — it just
# declares the directory as a package namespace.
